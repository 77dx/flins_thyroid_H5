
OSI七层模型

应用层：给用户提供服务接口

表示层：把数据翻译01001，加密压缩(非常容易出错)，

会话层：判断数据是否能网络传输

传输层：确定传输协议是否可靠(TCP/UDP，确定原端口号和接收端口),查错校验，流控

网络层：确定IP地址(原IP和目标IP),选路(自动选择通路)

数据链路层：用MAC地址访问媒介、错误检测和修正(MAC地址负责在局域网中传输)

物理层：设备之间的比特流的传输，物理接口，电气特性等
       0101001 bits
       网线，网卡
       